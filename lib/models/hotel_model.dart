class Hotel {
  int id, precio, categoria;
  String nombre, descripcion, direccion, imagen;

  Hotel(this.id, this.nombre, this.descripcion, this.direccion, this.precio,
      this.categoria, this.imagen);
}
